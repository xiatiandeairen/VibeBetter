# v0.17.0 Release Notes — Contextual Tips in CLI Commands

Helpful tips after every CLI command to improve discoverability.

- Random tip shown after `vibe check`, `vibe insights`, and `vibe status`
- 8 tips covering check, report, analyze, decisions, sync, risk workflows
- Dimmed text styling to avoid visual clutter
- Uses picocolors for consistent terminal output
