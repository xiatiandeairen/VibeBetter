# v0.18.0 Release Notes — Configurable Branding Constants

White-label foundation for enterprise deployments.

- `BRANDING` constant with name, tagline, primaryColor, logoText
- All values configurable via environment variables (BRAND_NAME, BRAND_TAGLINE, BRAND_COLOR, BRAND_LOGO_TEXT)
- Sensible defaults for standalone VibeBetter deployments
- Exported from `@vibebetter/shared` for frontend and backend consumption
