# v0.26.0 Release Notes — HTML Report Export

Enterprise-ready HTML export for AI Coding health reports.

- `vibe report --format html` generates a self-contained HTML page
- Dark theme with inline CSS (no external dependencies)
- Includes key metrics table with benchmark labels
- Ready for email, Confluence, or PDF conversion
