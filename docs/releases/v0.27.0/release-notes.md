# v0.27.0 Release Notes — Terminal Dashboard (TUI)

At-a-glance terminal dashboard for key project metrics.

- `vibe dashboard` prints a formatted TUI with all key metrics
- Displays AI Success Rate, Stable Rate, PSRI, TDI, PR counts, hotspots
- Integrates AI behavior stats (acceptance rate, edit distance, tools tracked)
- Pending decisions count from decision engine
- Color-coded values using benchmark thresholds
