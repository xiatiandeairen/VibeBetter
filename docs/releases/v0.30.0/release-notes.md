# v0.30.0 Release Notes — Changelog Command (Product)

CLI command listing recent commits with AI-authored detection.

- `vibe changelog` shows recent git commits with AI detection tags
- Scans commit messages and authors for AI patterns (copilot, cursor, gpt, claude, etc.)
- `--count` flag to control number of commits displayed
- Purple `[AI]` tag on detected AI-assisted commits
