# v0.35.0 Release Notes — Watch Mode (Product)

Live metric polling command for continuous monitoring.

- `vibe watch` polls project metrics at configurable interval (default 30s)
- Shows delta changes between polls for PSRI and TDI
- Color-coded metrics with benchmark comparison
- `--interval` flag to adjust poll frequency
