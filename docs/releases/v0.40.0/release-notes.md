# v0.40.0 Release Notes — File Search (Product)

CLI command to search files by name within project metrics.

- `vibe search <query>` finds files matching a name or path fragment
- Shows complexity, risk score, and lines of code for each match
- Searches against the full file metrics dataset
- `--count` flag to limit results
