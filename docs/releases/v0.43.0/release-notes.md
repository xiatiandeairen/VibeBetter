# v0.43.0 Release Notes — Badge Generator (Scenario)

CLI command generating shields.io markdown badges for READMEs.

- `vibe badge` generates a PSRI badge by default
- `--metric` flag supports psri, tdi, ai-success, or all
- Color-coded: green (healthy), yellow (warning), red (critical)
- Outputs copy-paste markdown image syntax
