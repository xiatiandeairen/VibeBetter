# v0.47.0 Release Notes — Validation Helpers (Maturity)

Common server-side validation utility functions.

- `isValidUrl()` validates HTTP/HTTPS URLs
- `isValidEmail()` validates email format
- `isValidProjectId()` validates alphanumeric project IDs (max 64 chars)
- `isNonEmptyString()` and `isPositiveInteger()` type guards
