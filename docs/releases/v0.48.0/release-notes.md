# v0.48.0 Release Notes — Export Config as YAML (Scenario)

CLI command to export configuration in YAML format.

- `vibe export-config` outputs current config as YAML to stdout
- `--output` flag to save directly to a file
- Includes generation timestamp in output header
- Simple YAML serializer with nested object support
