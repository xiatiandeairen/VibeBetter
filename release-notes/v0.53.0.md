# v0.53.0 — Deploy Check

**Category**: Scenario
**Date**: 2026-02-26

## Changes
- Added `vibe deploy-check` CLI command — pre-deployment risk assessment
- Evaluates PSRI, TDI, AI success rate, and hotspot ratio against thresholds
- `--strict` mode fails on any warning
- Configurable thresholds via `--psri-max` and `--tdi-max`
