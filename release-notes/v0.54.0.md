# v0.54.0 — Feature Flags

**Category**: Commercial
**Date**: 2026-02-26

## Changes
- Added `packages/shared/src/constants/feature-flags.ts` — FeatureFlag type + default flags
- 10 default flags across ai/ui/billing/integration/experimental categories
- `getDefaultFlags()` returns flag map, `isFeatureEnabled()` with fallback
- `getFlagsByCategory()` for filtered access
