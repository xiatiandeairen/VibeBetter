# v0.62.0 — CORS Configuration

**Category**: Maturity
**Date**: 2026-02-26

## Changes
- Added `apps/server/src/middleware/cors-config.ts` — centralized CORS middleware
- Environment-based origin parsing (CORS_ORIGINS / ALLOWED_ORIGINS)
- Production defaults to APP_URL, development allows localhost:3000/3001
- Exposes rate limit and request ID headers
