# v0.65.0 — Top Command

**Category**: Product
**Date**: 2026-02-26

## Changes
- Added `vibe top` CLI command — live-updating top files by risk (like unix `top`)
- Auto-refreshes at configurable interval (default 10s)
- Displays risk, complexity, change frequency, LOC, AI code ratio
- `--once` flag for single-shot output
