# v0.67.0 — Pagination Helpers

**Category**: Maturity
**Date**: 2026-02-26

## Changes
- Added `apps/server/src/utils/pagination.ts` — reusable cursor-based pagination
- Base64url cursor encoding/decoding with composite sort values
- `normalizePaginationParams()` with min/max limit enforcement
- `buildCursorResult()` generic helper for any `{ id }` entity
