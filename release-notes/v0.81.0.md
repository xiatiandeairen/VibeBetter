# v0.81.0 — Git Hooks Setup

**Category**: User
**Date**: 2026-02-26

## Changes
- Added `vibe setup-hooks` CLI command — one-command setup for pre-commit + pre-push hooks
- Installs VibeBetter risk checks as git hooks with bypass support
- `--uninstall` flag for clean removal of hooks
