# v0.83.0 — Docker Generator

**Category**: Scenario
**Date**: 2026-02-26

## Changes
- Added `vibe docker` CLI command — generate Docker setup for VibeBetter server
- Generates Dockerfile, docker-compose.yml, and .dockerignore
- Production-ready multi-stage build with PostgreSQL and Redis services
