# v1.10.0 — Data Flow

**Category**: Product
**Date**: 2026-02-26

## Changes
- Added `vibe flow` CLI command — visualize the VibeBetter data flow pipeline
- ASCII diagram showing Collect → Compute → Decide stages with metrics
- JSON output mode for integration
