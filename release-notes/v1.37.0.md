# v1.37.0 — JSON Schema Validation

**Category**: Maturity
**Date**: 2026-02-26

## Changes
- Added `apps/server/src/utils/json-schema.ts` — JSON Schema validation utility
- Validates types, strings, numbers, arrays, objects with full constraint support
- Supports required properties, pattern matching, and additional properties control
