# v1.43.0 — Datadog Integration

**Category**: Scenario
**Date**: 2026-02-26

## Changes
- Added `vibe datadog` CLI command — format metrics for Datadog custom metrics API
- Generates series payload with gauge/count types and project tags
- Configurable metric prefix and project key
