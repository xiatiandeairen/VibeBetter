# v2.6.0 — Offline Mode

**Category**: User
**Date**: 2026-02-26

## Changes
- Added `vibe offline` CLI command — cache latest metrics locally for offline access
- Save/load/clear cached snapshots with configurable TTL
- Works without server connection using local cache
